<% @page_title = "Section" %>
<%= link_to("<< Back to Menu", {:controller => 'access', :action => 'index'}, :class => 'back-link') %>
<div class="section index">
	<h2>Sections</h2>

	<%= link_to("Add New Section", {:action => 'new'}, :class => 'action new') %>

	<table class="listing" summary="Section list">
		<tr class="header">
			<th>&nbsp;</th>
			<th>Name</th>
			<th>Page</th>
			<th>Visable</th>
			<th>Content Type</th>
			<th>Content</th>
			<th>Actions</th>
		</tr>
		<% @section.each do |section| %>
		<tr>
			<td><%= section.position %></td>
			<td><%= section.name %></td>
            <td><%= section.page.name if section.page %></td>
            <td class="center"><%= status_tag(section.visable) %></td>
			<td><%= section.content_type %></td>
			<!-- Too much content for index
            <td><%= section.content %></td>
            Can be found in edit and show -->
			<td class="actions">
				<%= link_to("Show", {:action => 'show', :id => section.id}, :class => 'action show') %>
				<%= link_to("Edit", {:action => 'edit', :id => section.id}, :class => 'action edit') %>
				<%= link_to("Delete", {:action => 'delete', :id => section.id}, :class => 'action delete') %>
			</td>
		</tr>
		<% end %>
	</table>
</div>